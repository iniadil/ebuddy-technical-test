import { Typography } from "@mui/material";

export default function () {
  return (
    <Typography variant="overline" color="primary" fontSize={20}>
      <span className="bg-blue-500 py-2 px-3 rounded-xl text-white">
        Muhammad Adil's
      </span>{" "}
      Logic Form
    </Typography>
  );
}
